{% extends "_base.jinja" %}

{% block content %}

<div class="jumbotron">
    <h1 class="display-4">{{ _('404') }}</h1>
    <p class="lead">{{_('Page Not Found')}}</p>
    <hr class="my-4">
    <p>{{_('The resource you are looking for does not exist')}}</p>
    <a href="{{ url_for('index') }}" class="btn btn-primary">{{ _('Return to homepage') }}</a>
</div>    

{% endblock %}